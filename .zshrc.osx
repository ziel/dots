# -------------------------------------------------------------------------
# (o)>#|#<.>(o)>#|#<.>(o)>#|#<.>(o)>#|#<.>(o)>#|#<.>(o)>#|#<.>(o)>#|#<.>(o)
# -------------------------------------------------------------------------
# OSX Specials
# -------------------------------------------------------------------------

# Homebrew vim
alias vim=/usr/local/bin/vim

# Use personal fork of htop
alias htop="$HOME/bin/htop"

# This seems to be wanted on OSX
export TERM=xterm-256color

# 9 for OSX
export PLAN9=/opt/plan9
export PATH=$PATH:$PLAN9/bin

# Tree defaults for osx
# Depends: tree, less
function tree {
  /usr/bin/env tree -A "$@" | LESSCHARSET=IBM437 less
}

