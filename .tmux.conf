set-option -g prefix C-a
unbind C-a

setw -g mode-keys vi

bind-key a send-prefix
bind-key R source-file ~/.tmux.conf

bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

#
# @see: git@github.com:ChrisJohnsen/tmux-MacOSX-pasteboard.git
#
set-option -g default-command "reattach-to-user-namespace -l zsh"

set-option -g pane-active-border-fg black
set-option -g pane-border-fg black

set-option -g mouse-resize-pane on
set-option -g mouse-select-pane on
set-option -g mouse-select-window on

set-option -g message-command-bg black
set-option -g message-bg black

# zoom implementation 1 from SO
# bind C-k run "if [[ $(tmux list-window) =~ ZOOM ]]; then tmux last-window; tmux swap-pane -s ZOOM.1; tmux kill-window -t ZOOM; else tmux new-window -d -n ZOOM; tmux swap-pane -s ZOOM.1; tmux select-window -t ZOOM;fi"

# zoom  implementation 2 from SO:
# unbind +
# bind + new-window -d -n tmux-zoom 'clear && echo TMUX ZOOM && read' \; swap-pane -s tmux-zoom.0 \; select-window -t tmux-zoom
# unbind -
# bind - last-window \; swap-pane -s tmux-zoom.0 \; kill-window -t tmux-zoom

# ---
# solarized coloooorz
# ---

# default statusbar colors
set-option -g status-bg black #base02
set-option -g status-fg yellow #yellow
set-option -g status-attr default

# default window title colors
set-window-option -g window-status-fg brightblue #base0
set-window-option -g window-status-bg default

# active window title colors
set-window-option -g window-status-current-fg brightred #orange
set-window-option -g window-status-current-bg default

# pane border
set-option -g pane-border-fg black #base02
set-option -g pane-active-border-fg brightgreen #base01

# message text
set-option -g message-bg black #base02
set-option -g message-fg brightred #orange

# pane number display
set-option -g display-panes-active-colour blue #blue
set-option -g display-panes-colour brightred #orange

# clock
set-window-option -g clock-mode-colour green #green

# ---

set-option -g status on
set-option -g status-utf8 on
set-option -g status-interval 2
set-option -g status-justify "centre"
set-option -g status-left-length 60
set-option -g status-right-length 60
set-option -g status-left "#[fg=colour12,bg=colour0] tmux #[fg=colour4,bg=colour0] #(/bin/date) "
set-option -g status-right "#[fg=colour12,bg=colour0] #(/usr/bin/uptime | /usr/bin/awk '{print $10,$11,$12}') "


